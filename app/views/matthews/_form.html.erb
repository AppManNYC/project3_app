<%= form_with(model: matthew, local: true) do |form| %>
  <% if matthew.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(matthew.errors.count, "error") %> prohibited this matthew from being saved:</h2>

      <ul>
      <% matthew.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login, id: :matthew_login %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
